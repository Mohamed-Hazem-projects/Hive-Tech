export interface ILaptop {
  imageUrl: string,
  name: string,
  windows: string,
  cpu: string,
  gpu: string,
  ram: string,
  oldprice: number,
  discount: number,
  stock: number,
  ID: number,
  category: string,
  storage: string
}
